{
  "name": "build-update-management",
  "version": "1.0.0",
  "description": "this backend fetches info from docker hub and send notification about new builds",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ravi shankar",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.4.0",
    "dockerode": "^3.3.5",
    "dotenv": "^16.0.3"
  }
}
